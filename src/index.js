const encoder=new TextEncoder();
const fileTypes=new Map([["application/pdf","pdf"],["application/msword","doc"],["application/vnd.openxmlformats-officedocument.wordprocessingml.document","docx"]]);
const documentKinds=["cv","cover_letter","diploma","certificate","portfolio","other"];
const json=(data,status=200,headers={})=>new Response(JSON.stringify(data),{status,headers:{"content-type":"application/json; charset=utf-8",...headers}});
const bad=(message,status=400)=>json({error:message},status);
const now=()=>new Date().toISOString();
const future=days=>new Date(Date.now()+days*86400000).toISOString();
const clean=(value,max=500)=>typeof value==="string"&&value.trim().length<=max?value.trim():"";
const list=value=>Array.isArray(value)?value.map(item=>typeof item==="string"?clean(item,160):item).filter(Boolean):[];
const parseStored=(value,fallback)=>{try{return typeof value==="string"?JSON.parse(value):value??fallback}catch{return fallback}};
const cors=request=>({"access-control-allow-origin":new URL(request.url).origin,"access-control-allow-credentials":"true","access-control-allow-headers":"content-type, x-turnstile-token","access-control-allow-methods":"GET,POST,PATCH,DELETE,OPTIONS"});
const validPhone=value=>/^\+212[5-7]\d{8}$/.test(String(value||"").replace(/[\s.-]/g,""));
const validPassword=value=>typeof value==="string"&&value.length>=8&&/[a-z]/.test(value)&&/[A-Z]/.test(value)&&/\d/.test(value)&&/[^A-Za-z0-9]/.test(value);
function token(){const bytes=crypto.getRandomValues(new Uint8Array(32));return btoa(String.fromCharCode(...bytes)).replaceAll("+","-").replaceAll("/","_").replaceAll("=","")}
async function digest(value){const bytes=await crypto.subtle.digest("SHA-256",encoder.encode(value));return [...new Uint8Array(bytes)].map(x=>x.toString(16).padStart(2,"0")).join("")}
async function hashPassword(password,salt){const key=await crypto.subtle.importKey("raw",encoder.encode(password),"PBKDF2",false,["deriveBits"]);const bits=await crypto.subtle.deriveBits({name:"PBKDF2",salt:encoder.encode(salt),iterations:310000,hash:"SHA-256"},key,256);return btoa(String.fromCharCode(...new Uint8Array(bits)))}
const cookie=(name,value,maxAge=0)=>`${name}=${value}; Path=/; HttpOnly; Secure; SameSite=Lax; Max-Age=${maxAge}`;
const sessionToken=request=>request.headers.get("cookie")?.match(/(?:^|;\s*)wc_session=([^;]+)/)?.[1]||null;
async function userFor(request,env){const raw=sessionToken(request);if(!raw)return null;return env.DB.prepare("SELECT u.id,u.email,u.role,u.email_verified_at,s.id session_id,s.token_hash FROM sessions s JOIN users u ON u.id=s.user_id WHERE s.token_hash=? AND s.expires_at>?").bind(await digest(raw+env.SESSION_PEPPER),now()).first()}
async function requireUser(request,env,roles){const user=await userFor(request,env);if(!user)throw bad("Authentification requise.",401);if(roles&&!roles.includes(user.role))throw bad("Accès interdit.",403);return user}
async function createSession(env,userId){const raw=token();await env.DB.prepare("INSERT INTO sessions(id,user_id,token_hash,expires_at) VALUES(?,?,?,?)").bind(crypto.randomUUID(),userId,await digest(raw+env.SESSION_PEPPER),future(30)).run();return raw}
async function audit(env,user,action,type,id,metadata={}){await env.DB.prepare("INSERT INTO audit_logs(id,actor_user_id,action,resource_type,resource_id,metadata_json) VALUES(?,?,?,?,?,?)").bind(crypto.randomUUID(),user?.id||null,action,type,id||null,JSON.stringify(metadata)).run()}
async function profile(env,user){if(user.role==="candidate")return env.DB.prepare("SELECT first_name,last_name,phone,city,region,country,preferred_language,professional_title,introduction,availability,availability_details,profile_visible,skills_json,preferences_json,experience_json,education_json,languages_json,questionnaire_answers,updated_at FROM candidate_profiles WHERE user_id=?").bind(user.id).first();if(user.role==="recruiter")return env.DB.prepare("SELECT first_name,last_name,phone,company_name,job_title,company_sector,company_size,city,website FROM recruiter_profiles WHERE user_id=?").bind(user.id).first();return null}
async function emailToken(env,userId,purpose){const raw=token();await env.DB.prepare("INSERT INTO email_tokens(id,user_id,token_hash,purpose,expires_at) VALUES(?,?,?,?,?)").bind(crypto.randomUUID(),userId,await digest(raw+env.SESSION_PEPPER),purpose,future(1)).run();return raw}
async function sendEmail(env,message){if(env.EMAIL_PROVIDER_API_KEY&&env.EMAIL_FROM)console.log(JSON.stringify({event:"email_queued",to:message.to,template:message.template}))}

async function register(request,env){const body=await request.json().catch(()=>null);if(!body)return bad("Données invalides.");const role=["candidate","recruiter"].includes(body.role)?body.role:null,email=clean(body.email,254).toLowerCase(),first=clean(body.firstName,80),last=clean(body.lastName,80),phone=clean(body.phone,20).replace(/[\s.-]/g,"");if(!role||!first||!last||!/^\S+@\S+\.\S+$/.test(email)||!validPhone(phone)||!validPassword(body.password)||body.acceptedTerms!==true)return bad("Veuillez vérifier les informations du formulaire.");if(body.password!==body.confirmPassword)return bad("Les mots de passe ne correspondent pas.");if(await env.DB.prepare("SELECT id FROM users WHERE email=?").bind(email).first())return bad("Impossible de créer ce compte avec ces informations.",409);const id=crypto.randomUUID(),salt=token(),hash=await hashPassword(body.password,salt);await env.DB.batch([env.DB.prepare("INSERT INTO users(id,email,password_hash,password_salt,role) VALUES(?,?,?,?,?)").bind(id,email,hash,salt,role),role==="candidate"?env.DB.prepare("INSERT INTO candidate_profiles(user_id,first_name,last_name,phone,preferred_language) VALUES(?,?,?,?,?)").bind(id,first,last,phone,["fr","en","ar"].includes(body.language)?body.language:"fr"):env.DB.prepare("INSERT INTO recruiter_profiles(user_id,first_name,last_name,phone,company_name,job_title) VALUES(?,?,?,?,?,?)").bind(id,first,last,phone,clean(body.companyName,120)||null,clean(body.jobTitle,120)||null),env.DB.prepare("INSERT INTO notifications(id,user_id,type,title,body) VALUES(?,?,?,?,?)").bind(crypto.randomUUID(),id,"account","Bienvenue sur Workcrute","Complétez votre profil pour améliorer vos opportunités.")]);const verify=await emailToken(env,id,"verify_email");await sendEmail(env,{to:email,template:"verify_email",token:verify});const session=await createSession(env,id);return json({user:{id,email,role},emailVerificationPending:true},201,{"set-cookie":cookie("wc_session",session,2592000)})}
async function login(request,env){const body=await request.json().catch(()=>({})),email=clean(body.email,254).toLowerCase();const user=await env.DB.prepare("SELECT * FROM users WHERE email=?").bind(email).first();if(!user||typeof body.password!=="string")return bad("Identifiants invalides.",401);const hash=await hashPassword(body.password,user.password_salt);if(hash!==user.password_hash)return bad("Identifiants invalides.",401);const session=await createSession(env,user.id);return json({user:{id:user.id,email:user.email,role:user.role}},200,{"set-cookie":cookie("wc_session",session,2592000)})}
async function authAction(request,env,purpose){const body=await request.json().catch(()=>({})),email=clean(body.email,254).toLowerCase(),user=email?await env.DB.prepare("SELECT id,email FROM users WHERE email=?").bind(email).first():null;if(user){const value=await emailToken(env,user.id,purpose);await sendEmail(env,{to:user.email,template:purpose,token:value})}return json({ok:true})}
async function me(request,env){const user=await requireUser(request,env);return json({user:{id:user.id,email:user.email,role:user.role,emailVerified:Boolean(user.email_verified_at)},profile:await profile(env,user)})}
async function updateProfile(request,env){const user=await requireUser(request,env);const body=await request.json().catch(()=>null);if(!body)return bad("Données invalides.");if(user.role==="recruiter"){await env.DB.prepare("UPDATE recruiter_profiles SET company_name=?,job_title=?,company_sector=?,company_size=?,city=?,website=?,updated_at=CURRENT_TIMESTAMP WHERE user_id=?").bind(clean(body.companyName,160)||null,clean(body.jobTitle,120)||null,clean(body.companySector,120)||null,clean(body.companySize,80)||null,clean(body.city,120)||null,clean(body.website,240)||null,user.id).run();return me(request,env)}if(user.role!=="candidate")return bad("Profil candidat requis.",403);const previous=await profile(env,user),availability=body.availability===undefined?(previous.availability||""):(["immediate","one_month","two_months","other",""].includes(body.availability)?body.availability:"");if(availability==="other"&&!clean(body.availabilityDetails??previous.availability_details,160))return bad("Précisez votre disponibilité.");const first=clean(body.firstName??previous.first_name,80),last=clean(body.lastName??previous.last_name,80),phone=clean(body.phone??previous.phone,20);if(!first||!last||!validPhone(phone))return bad("Vérifiez votre identité et votre téléphone.");const oldList=(key)=>parseStored(previous[key],[]),oldObject=(key)=>parseStored(previous[key],{});await env.DB.prepare("UPDATE candidate_profiles SET first_name=?,last_name=?,phone=?,city=?,region=?,country=?,preferred_language=?,professional_title=?,introduction=?,availability=?,availability_details=?,skills_json=?,preferences_json=?,experience_json=?,education_json=?,languages_json=?,questionnaire_answers=?,updated_at=CURRENT_TIMESTAMP WHERE user_id=?").bind(first,last,phone.replace(/[\s.-]/g,""),clean(body.city??previous.city,120)||null,clean(body.region??previous.region,120)||null,clean(body.country??previous.country,120)||null,["fr","en","ar"].includes(body.language)?body.language:previous.preferred_language||"fr",clean(body.professionalTitle??previous.professional_title,120)||null,clean(body.introduction??previous.introduction,1000)||null,availability||null,availability==="other"?clean(body.availabilityDetails??previous.availability_details,160):null,JSON.stringify(body.skills===undefined?oldList("skills_json"):list(body.skills)),JSON.stringify(body.preferences===undefined?oldObject("preferences_json"):(body.preferences&&typeof body.preferences==="object"?body.preferences:{})),JSON.stringify(body.experience===undefined?oldList("experience_json"):list(body.experience)),JSON.stringify(body.education===undefined?oldList("education_json"):list(body.education)),JSON.stringify(body.languages===undefined?oldList("languages_json"):list(body.languages)),JSON.stringify(body.questionnaireAnswers===undefined?oldObject("questionnaire_answers"):(body.questionnaireAnswers&&typeof body.questionnaireAnswers==="object"?body.questionnaireAnswers:{})),user.id).run();await audit(env,user,"profile_updated","candidate_profile",user.id);return me(request,env)}

async function documents(request,env,path){const user=await requireUser(request,env,["candidate"]);if(path==="/api/documents"&&request.method==="GET"){const {results=[]}=await env.DB.prepare("SELECT id,kind,original_name,content_type,size_bytes,is_default,created_at FROM documents WHERE user_id=? AND deleted_at IS NULL ORDER BY created_at DESC").bind(user.id).all();return json({items:results})}if(path==="/api/documents"&&request.method==="POST"){const form=await request.formData(),file=form.get("file"),kind=String(form.get("kind")||"");if(!(file instanceof File)||!documentKinds.includes(kind))return bad("Document invalide.");const ext=fileTypes.get(file.type);if(!ext||!file.size||file.size>8*1024*1024||!file.name.toLowerCase().endsWith("."+ext))return bad("Le document doit être un PDF, DOC ou DOCX de moins de 8 Mo.");const id=crypto.randomUUID(),key=`private/${user.id}/${id}.${ext}`;await env.DOCUMENTS.put(key,file.stream(),{httpMetadata:{contentType:file.type},customMetadata:{owner:user.id,kind}});const count=await env.DB.prepare("SELECT COUNT(*) total FROM documents WHERE user_id=? AND kind='cv' AND deleted_at IS NULL").bind(user.id).first();const isDefault=kind==="cv"&&count.total===0?1:0;await env.DB.prepare("INSERT INTO documents(id,user_id,kind,storage_key,original_name,content_type,size_bytes,is_default) VALUES(?,?,?,?,?,?,?,?)").bind(id,user.id,kind,key,file.name,file.type,file.size,isDefault).run();await audit(env,user,"document_uploaded","document",id,{kind});return json({document:{id,kind,original_name:file.name,is_default:isDefault}},201)}const match=path.match(/^\/api\/documents\/([^/]+)(?:\/(download|default))?$/);if(!match)return bad("Action non prise en charge.",405);const [,id,action]=match,doc=await env.DB.prepare("SELECT * FROM documents WHERE id=? AND user_id=? AND deleted_at IS NULL").bind(id,user.id).first();if(!doc)return bad("Document introuvable.",404);if(action==="download"&&request.method==="GET"){const object=await env.DOCUMENTS.get(doc.storage_key);if(!object)return bad("Fichier introuvable.",404);return new Response(object.body,{headers:{"content-type":doc.content_type,"content-disposition":`attachment; filename*=UTF-8''${encodeURIComponent(doc.original_name)}`}})}if(action==="default"&&request.method==="PATCH"){if(doc.kind!=="cv")return bad("Seul un CV peut être principal.");await env.DB.batch([env.DB.prepare("UPDATE documents SET is_default=0 WHERE user_id=? AND kind='cv'").bind(user.id),env.DB.prepare("UPDATE documents SET is_default=1 WHERE id=?").bind(id)]);return json({ok:true})}if(!action&&request.method==="DELETE"){await env.DOCUMENTS.delete(doc.storage_key);await env.DB.prepare("UPDATE documents SET deleted_at=CURRENT_TIMESTAMP,is_default=0 WHERE id=?").bind(id).run();return new Response(null,{status:204})}return bad("Action non prise en charge.",405)}
async function notifications(request,env,path){const user=await requireUser(request,env);if(path==="/api/notifications"&&request.method==="GET"){const {results=[]}=await env.DB.prepare("SELECT id,type,title,body,href,read_at,created_at FROM notifications WHERE user_id=? ORDER BY created_at DESC LIMIT 50").bind(user.id).all();return json({items:results})}if(path==="/api/notifications"&&request.method==="POST"){await env.DB.prepare("UPDATE notifications SET read_at=CURRENT_TIMESTAMP WHERE user_id=?").bind(user.id).run();return new Response(null,{status:204})}const id=path.split("/").pop();if(request.method==="PATCH"){const body=await request.json().catch(()=>({}));await env.DB.prepare(body.read===false?"UPDATE notifications SET read_at=NULL WHERE id=? AND user_id=?":"UPDATE notifications SET read_at=CURRENT_TIMESTAMP WHERE id=? AND user_id=?").bind(id,user.id).run();return json({ok:true})}if(request.method==="DELETE"){await env.DB.prepare("DELETE FROM notifications WHERE id=? AND user_id=?").bind(id,user.id).run();return new Response(null,{status:204})}return bad("Action non prise en charge.",405)}
async function candidateStats(request,env){const user=await requireUser(request,env,["candidate"]);const row=await env.DB.prepare("SELECT (SELECT COUNT(*) FROM applications WHERE candidate_user_id=?) applications_sent,(SELECT COUNT(*) FROM applications WHERE candidate_user_id=? AND status IN ('reviewing','shortlisted','interview')) applications_active,(SELECT COUNT(*) FROM interviews WHERE candidate_user_id=? AND starts_at>CURRENT_TIMESTAMP AND status IN ('scheduled','confirmed')) interviews,(SELECT COUNT(DISTINCT viewer_user_id) FROM profile_views WHERE profile_owner_id=? AND viewer_role='recruiter' AND viewer_user_id IS NOT NULL) recruiter_views").bind(user.id,user.id,user.id,user.id).first();return json({applicationsSent:row.applications_sent||0,applicationsActive:row.applications_active||0,interviews:row.interviews||0,recruiterViews:row.recruiter_views||0})}

async function jobs(request,env,path){const url=new URL(request.url),user=await userFor(request,env);if(request.method==="GET"&&path==="/api/jobs"){let sql="SELECT j.id,j.title,j.domain,j.description,j.contract_type,j.city,j.work_mode,j.experience_level,j.published_at,c.name company_name"+(user?.role==="candidate"?",EXISTS(SELECT 1 FROM saved_jobs s WHERE s.job_offer_id=j.id AND s.user_id=?) is_saved":"")+" FROM job_offers j LEFT JOIN companies c ON c.id=j.company_id WHERE j.status='published'",params=user?.role==="candidate"?[user.id]:[];for(const [value,clause,count] of [[clean(url.searchParams.get("q"),100)," AND (j.title LIKE ? OR j.description LIKE ? OR j.domain LIKE ?)",3],[clean(url.searchParams.get("domain"),100)," AND j.domain=?",1],[clean(url.searchParams.get("city"),100)," AND j.city=?",1],[clean(url.searchParams.get("contract"),50)," AND j.contract_type=?",1]])if(value){sql+=clause;params.push(...Array(count).fill(count===3?`%${value}%`:value))}const {results=[]}=await env.DB.prepare(sql+" ORDER BY j.published_at DESC LIMIT 50").bind(...params).all();return json({items:results})}if(request.method==="GET"&&path.startsWith("/api/jobs/")){const id=path.split("/").pop();const job=await env.DB.prepare("SELECT j.*,c.name company_name"+(user?.role==="candidate"?",EXISTS(SELECT 1 FROM saved_jobs s WHERE s.job_offer_id=j.id AND s.user_id=?) is_saved":"")+" FROM job_offers j LEFT JOIN companies c ON c.id=j.company_id WHERE j.id=? AND (j.status='published' OR j.recruiter_user_id=?)").bind(...(user?.role==="candidate"?[user.id]:[]),id,user?.id||"").first();return job?json({job,matchingScore:null}):bad("Offre introuvable.",404)}const actor=await requireUser(request,env,["recruiter"]);if(path==="/api/jobs"&&request.method==="POST"){const body=await request.json().catch(()=>null);if(!body)return bad("Données invalides.");const title=clean(body.title,160),domain=clean(body.domain,120),description=clean(body.description,5000),contract=clean(body.contractType,80),city=clean(body.city,120),mode=clean(body.workMode,40);if(!title||!domain||!description||!contract||!city||!mode)return bad("Veuillez renseigner les champs obligatoires.");let company=await env.DB.prepare("SELECT id FROM companies WHERE owner_user_id=?").bind(actor.id).first();if(!company){company={id:crypto.randomUUID()};await env.DB.prepare("INSERT INTO companies(id,owner_user_id,name,sector,city) VALUES(?,?,?,?,?)").bind(company.id,actor.id,clean(body.companyName,160)||"Entreprise",clean(body.companySector,120)||null,city).run()}const id=crypto.randomUUID(),status=body.status==="published"?"published":"draft";await env.DB.prepare("INSERT INTO job_offers(id,recruiter_user_id,company_id,title,domain,description,missions,required_skills,contract_type,city,work_mode,status,published_at) VALUES(?,?,?,?,?,?,?,?,?,?,?,?,?)").bind(id,actor.id,company.id,title,domain,description,clean(body.missions,5000)||null,JSON.stringify(list(body.skills)),contract,city,mode,status,status==="published"?now():null).run();return json({job:{id,status}},201)}return bad("Action non prise en charge.",405)}
async function savedJobs(request,env,path){const user=await requireUser(request,env,["candidate"]);if(path==="/api/saved-jobs"&&request.method==="GET"){const {results=[]}=await env.DB.prepare("SELECT j.id,j.title,j.domain,j.description,j.contract_type,j.city,j.work_mode,j.published_at,c.name company_name,1 is_saved FROM saved_jobs s JOIN job_offers j ON j.id=s.job_offer_id LEFT JOIN companies c ON c.id=j.company_id WHERE s.user_id=? AND j.status='published' ORDER BY s.created_at DESC").bind(user.id).all();return json({items:results})}const jobId=path.split("/").pop();if(request.method==="POST"){if(!await env.DB.prepare("SELECT id FROM job_offers WHERE id=? AND status='published'").bind(jobId).first())return bad("Offre introuvable.",404);await env.DB.prepare("INSERT OR IGNORE INTO saved_jobs(user_id,job_offer_id) VALUES(?,?)").bind(user.id,jobId).run();return json({saved:true},201)}if(request.method==="DELETE"){await env.DB.prepare("DELETE FROM saved_jobs WHERE user_id=? AND job_offer_id=?").bind(user.id,jobId).run();return new Response(null,{status:204})}return bad("Action non prise en charge.",405)}
async function alerts(request,env,path){const user=await requireUser(request,env,["candidate"]);if(path==="/api/job-alerts"&&request.method==="GET"){const {results=[]}=await env.DB.prepare("SELECT * FROM job_alerts WHERE user_id=? ORDER BY created_at DESC").bind(user.id).all();return json({items:results})}if(path==="/api/job-alerts"&&request.method==="POST"){const body=await request.json().catch(()=>null);if(!body)return bad("Données invalides.");if(!clean(body.keywords,160)&&!clean(body.domain,120)&&!clean(body.city,120)&&!clean(body.contractType,80)&&!list(body.skills).length)return bad("Choisissez au moins un critère.");const id=crypto.randomUUID();await env.DB.prepare("INSERT INTO job_alerts(id,user_id,name,keywords,domain,city,contract_type,work_mode,skills_json,frequency,in_app_enabled,email_enabled) VALUES(?,?,?,?,?,?,?,?,?,?,?,?)").bind(id,user.id,clean(body.name,120)||null,clean(body.keywords,160)||null,clean(body.domain,120)||null,clean(body.city,120)||null,clean(body.contractType,80)||null,clean(body.workMode,80)||null,JSON.stringify(list(body.skills)),["immediate","daily","weekly"].includes(body.frequency)?body.frequency:"daily",body.inApp===false?0:1,body.email?1:0).run();return json({alert:{id}},201)}const id=path.split("/").pop();if(request.method==="PATCH"){const body=await request.json().catch(()=>({}));await env.DB.prepare("UPDATE job_alerts SET is_active=COALESCE(?,is_active),frequency=COALESCE(?,frequency),updated_at=CURRENT_TIMESTAMP WHERE id=? AND user_id=?").bind(typeof body.active==="boolean"?(body.active?1:0):null,["immediate","daily","weekly"].includes(body.frequency)?body.frequency:null,id,user.id).run();return json({ok:true})}if(request.method==="DELETE"){await env.DB.prepare("DELETE FROM job_alerts WHERE id=? AND user_id=?").bind(id,user.id).run();return new Response(null,{status:204})}return bad("Action non prise en charge.",405)}
async function applications(request,env,path){const user=await requireUser(request,env,["candidate"]);if(path==="/api/applications"&&request.method==="GET"){const {results=[]}=await env.DB.prepare("SELECT a.id,a.status,a.created_at,a.updated_at,j.id job_id,j.title,j.city,j.contract_type,c.name company_name FROM applications a JOIN job_offers j ON j.id=a.job_offer_id LEFT JOIN companies c ON c.id=j.company_id WHERE a.candidate_user_id=? ORDER BY a.created_at DESC").bind(user.id).all();return json({items:results})}if(path==="/api/applications"&&request.method==="POST"){const body=await request.json().catch(()=>({})),jobId=clean(body.jobId,80),job=await env.DB.prepare("SELECT id,recruiter_user_id,title FROM job_offers WHERE id=? AND status='published'").bind(jobId).first();if(!job)return bad("Offre introuvable.",404);try{const id=crypto.randomUUID();await env.DB.batch([env.DB.prepare("INSERT INTO applications(id,job_offer_id,candidate_user_id,cover_letter) VALUES(?,?,?,?)").bind(id,jobId,user.id,clean(body.coverLetter,3000)||null),env.DB.prepare("INSERT INTO application_status_history(id,application_id,actor_user_id,status) VALUES(?,?,?,?)").bind(crypto.randomUUID(),id,user.id,"submitted"),env.DB.prepare("INSERT INTO notifications(id,user_id,type,title,body,href) VALUES(?,?,?,?,?,?)").bind(crypto.randomUUID(),job.recruiter_user_id,"application","Nouvelle candidature",`Une candidature a été reçue pour ${job.title}`,"/recruteur/candidats")]);return json({application:{id,status:"submitted"}},201)}catch(error){if(String(error).includes("UNIQUE"))return bad("Vous avez déjà postulé à cette offre.",409);throw error}}const id=path.split("/").pop();const application=await env.DB.prepare("SELECT a.*,j.title,j.description,j.city,j.contract_type,j.work_mode,c.name company_name FROM applications a JOIN job_offers j ON j.id=a.job_offer_id LEFT JOIN companies c ON c.id=j.company_id WHERE a.id=? AND a.candidate_user_id=?").bind(id,user.id).first();if(!application)return bad("Candidature introuvable.",404);if(request.method==="GET"){const {results=[]}=await env.DB.prepare("SELECT status,created_at FROM application_status_history WHERE application_id=? ORDER BY created_at").bind(id).all();return json({application,timeline:results})}if(request.method==="PATCH"){if(application.status==="accepted"||application.status==="rejected")return bad("Cette candidature ne peut plus être retirée.");await env.DB.batch([env.DB.prepare("UPDATE applications SET status='withdrawn',updated_at=CURRENT_TIMESTAMP WHERE id=?").bind(id),env.DB.prepare("INSERT INTO application_status_history(id,application_id,actor_user_id,status) VALUES(?,?,?,'withdrawn')").bind(crypto.randomUUID(),id,user.id)]);return json({ok:true})}return bad("Action non prise en charge.",405)}
async function interviews(request,env,path){const user=await requireUser(request,env,["candidate"]);if(path==="/api/interviews"&&request.method==="GET"){const {results=[]}=await env.DB.prepare("SELECT i.*,j.title,c.name company_name FROM interviews i JOIN applications a ON a.id=i.application_id JOIN job_offers j ON j.id=a.job_offer_id LEFT JOIN companies c ON c.id=j.company_id WHERE i.candidate_user_id=? ORDER BY i.starts_at").bind(user.id).all();return json({items:results})}if(request.method==="PATCH"){const body=await request.json().catch(()=>({})),status=["confirmed","declined","reschedule_requested"].includes(body.status)?body.status:null;if(!status)return bad("Statut invalide.");await env.DB.prepare("UPDATE interviews SET status=?,candidate_note=?,updated_at=CURRENT_TIMESTAMP WHERE id=? AND candidate_user_id=?").bind(status,clean(body.note,500)||null,path.split("/").pop(),user.id).run();return json({ok:true})}return bad("Action non prise en charge.",405)}
async function settings(request,env){const user=await requireUser(request,env,["candidate"]);if(request.method==="GET"){const prefs=await env.DB.prepare("SELECT * FROM notification_preferences WHERE user_id=?").bind(user.id).first();const profileRow=await env.DB.prepare("SELECT profile_visible,preferred_language FROM candidate_profiles WHERE user_id=?").bind(user.id).first();return json({settings:{inAppEnabled:prefs?.in_app_enabled??1,emailEnabled:prefs?.email_enabled??1,jobAlertsEnabled:prefs?.job_alerts_enabled??1,profileViewEnabled:prefs?.profile_view_enabled??0,profileVisible:profileRow.profile_visible,language:profileRow.preferred_language}})}const body=await request.json().catch(()=>({}));await env.DB.batch([env.DB.prepare("INSERT INTO notification_preferences(user_id,in_app_enabled,email_enabled,job_alerts_enabled,profile_view_enabled) VALUES(?,?,?,?,?) ON CONFLICT(user_id) DO UPDATE SET in_app_enabled=excluded.in_app_enabled,email_enabled=excluded.email_enabled,job_alerts_enabled=excluded.job_alerts_enabled,profile_view_enabled=excluded.profile_view_enabled,updated_at=CURRENT_TIMESTAMP").bind(user.id,body.inAppEnabled?1:0,body.emailEnabled?1:0,body.jobAlertsEnabled?1:0,body.profileViewEnabled?1:0),env.DB.prepare("UPDATE candidate_profiles SET profile_visible=?,preferred_language=?,updated_at=CURRENT_TIMESTAMP WHERE user_id=?").bind(body.profileVisible?1:0,["fr","en","ar"].includes(body.language)?body.language:"fr",user.id)]);return json({ok:true})}
async function security(request,env){const user=await requireUser(request,env);const body=await request.json().catch(()=>({}));if(!validPassword(body.newPassword)||body.newPassword!==body.confirmPassword)return bad("Le nouveau mot de passe est invalide ou ne correspond pas.");const current=await env.DB.prepare("SELECT password_hash,password_salt FROM users WHERE id=?").bind(user.id).first();if(await hashPassword(body.currentPassword,current.password_salt)!==current.password_hash)return bad("Le mot de passe actuel est incorrect.",401);const salt=token(),hash=await hashPassword(body.newPassword,salt);await env.DB.batch([env.DB.prepare("UPDATE users SET password_hash=?,password_salt=?,updated_at=CURRENT_TIMESTAMP WHERE id=?").bind(hash,salt,user.id),env.DB.prepare("DELETE FROM sessions WHERE user_id=? AND id<>?").bind(user.id,user.session_id)]);return json({ok:true})}
async function overview(request,env){const user=await requireUser(request,env,["candidate"]);const [profileRow,stats,notificationsResult,applicationsResult,interviewsResult,jobsResult]=await Promise.all([profile(env,user),candidateStats(request,env).then(r=>r.json()),notifications(request,env,"/api/notifications").then(r=>r.json()),applications(request,env,"/api/applications").then(r=>r.json()),interviews(request,env,"/api/interviews").then(r=>r.json()),jobs(request,env,"/api/jobs").then(r=>r.json())]);return json({profile:profileRow,stats,notifications:notificationsResult.items.slice(0,5),applications:applicationsResult.items.slice(0,5),interviews:interviewsResult.items.filter(i=>new Date(i.starts_at)>new Date()).slice(0,3),recommendedJobs:jobsResult.items.slice(0,4),matchingScore:null})}
async function publicStats(env){const row=await env.DB.prepare("SELECT (SELECT COUNT(*) FROM users WHERE role='candidate') candidates,(SELECT COUNT(*) FROM companies) companies,(SELECT COUNT(*) FROM job_offers WHERE status='published') jobs,(SELECT COUNT(*) FROM applications) applications").first();return json({stats:row||{}})}
async function questionnaire(env){const {results=[]}=await env.DB.prepare("SELECT id,field_key,type,labels_json,description_json,options_json,sort_order,is_required FROM questionnaire_questions WHERE is_active=1 ORDER BY sort_order").all();return json(results.map(q=>({...q,labels:JSON.parse(q.labels_json),options:q.options_json?JSON.parse(q.options_json):[]})))}
async function recruiterJobs(request,env){const user=await requireUser(request,env,["recruiter"]);const {results=[]}=await env.DB.prepare("SELECT j.*,c.name company_name FROM job_offers j LEFT JOIN companies c ON c.id=j.company_id WHERE j.recruiter_user_id=? ORDER BY j.created_at DESC").bind(user.id).all();return json({items:results})}
async function adminStats(request,env){await requireUser(request,env,["admin"]);const row=await env.DB.prepare("SELECT (SELECT COUNT(*) FROM users) users,(SELECT COUNT(*) FROM users WHERE role='candidate') candidates,(SELECT COUNT(*) FROM users WHERE role='recruiter') recruiters,(SELECT COUNT(*) FROM job_offers WHERE status='published') active_jobs,(SELECT COUNT(*) FROM applications) applications").first();return json({stats:row})}
async function adminQuestionnaire(request,env,path){const user=await requireUser(request,env,["admin"]);if(request.method==="GET"){const {results=[]}=await env.DB.prepare("SELECT id,field_key,type,labels_json,description_json,options_json,sort_order,is_required,is_active FROM questionnaire_questions ORDER BY sort_order").all();return json({items:results.map(row=>({...row,labels:parseStored(row.labels_json,{}),options:parseStored(row.options_json,[])}))})}const body=await request.json().catch(()=>null);if(!body)return bad("Données invalides.");if(path==="/api/admin/questionnaire"&&request.method==="POST"){const field=clean(body.fieldKey,80).toLowerCase().replace(/[^a-z0-9_]/g,"_"),label=clean(body.label,160),type=["text","textarea","number","select","multiselect"].includes(body.type)?body.type:null,options=list(body.options);if(!field||!label||!type||(["select","multiselect"].includes(type)&&!options.length))return bad("Veuillez renseigner une question valide.");const id=crypto.randomUUID(),last=await env.DB.prepare("SELECT COALESCE(MAX(sort_order),0) max_sort FROM questionnaire_questions").first();await env.DB.prepare("INSERT INTO questionnaire_questions(id,field_key,type,labels_json,options_json,sort_order,is_required,is_active) VALUES(?,?,?,?,?,?,?,1)").bind(id,field,type,JSON.stringify({fr:label,en:clean(body.labelEn,160)||label,ar:clean(body.labelAr,160)||label}),JSON.stringify(options),(last.max_sort||0)+10,body.required?1:0).run();await audit(env,user,"question_created","questionnaire_question",id);return json({id},201)}const id=path.split("/").pop();if(request.method==="PATCH"){const label=clean(body.label,160),options=Array.isArray(body.options)?list(body.options):null;await env.DB.prepare("UPDATE questionnaire_questions SET labels_json=COALESCE(?,labels_json),options_json=COALESCE(?,options_json),sort_order=COALESCE(?,sort_order),is_required=COALESCE(?,is_required),is_active=COALESCE(?,is_active) WHERE id=?").bind(label?JSON.stringify({fr:label,en:clean(body.labelEn,160)||label,ar:clean(body.labelAr,160)||label}):null,options?JSON.stringify(options):null,Number.isInteger(body.sortOrder)?body.sortOrder:null,typeof body.required==="boolean"?(body.required?1:0):null,typeof body.active==="boolean"?(body.active?1:0):null,id).run();return json({ok:true})}if(request.method==="DELETE"){await env.DB.prepare("UPDATE questionnaire_questions SET is_active=0 WHERE id=?").bind(id).run();return new Response(null,{status:204})}return bad("Action non prise en charge.",405)}

export default {async fetch(request,env){
  const path=new URL(request.url).pathname;
  if(request.method==="OPTIONS")return new Response(null,{headers:cors(request)});
  try{
    let response;
    if(path==="/api/public/stats")response=await publicStats(env);
    else if(path==="/api/auth/register"&&request.method==="POST")response=await register(request,env);
    else if(path==="/api/auth/login"&&request.method==="POST")response=await login(request,env);
    else if(path==="/api/auth/logout"&&request.method==="POST"){const raw=sessionToken(request);if(raw)await env.DB.prepare("DELETE FROM sessions WHERE token_hash=?").bind(await digest(raw+env.SESSION_PEPPER)).run();response=json({ok:true},200,{"set-cookie":cookie("wc_session","")})}
    else if(path==="/api/auth/me")response=await me(request,env);
    else if(path==="/api/auth/forgot-password"&&request.method==="POST")response=await authAction(request,env,"reset_password");
    else if(path==="/api/auth/resend-verification"&&request.method==="POST")response=await authAction(request,env,"verify_email");
    else if(path==="/api/auth/change-password"&&request.method==="POST")response=await security(request,env);
    else if(path==="/api/questionnaire")response=await questionnaire(env);
    else if(path==="/api/profile"&&request.method==="PATCH")response=await updateProfile(request,env);
    else if(path==="/api/candidate/overview")response=await overview(request,env);
    else if(path==="/api/candidate/stats")response=await candidateStats(request,env);
    else if(path==="/api/candidate/settings")response=await settings(request,env);
    else if(path==="/api/documents"||path.startsWith("/api/documents/"))response=await documents(request,env,path);
    else if(path==="/api/notifications"||path.startsWith("/api/notifications/"))response=await notifications(request,env,path);
    else if(path==="/api/saved-jobs"||path.startsWith("/api/saved-jobs/"))response=await savedJobs(request,env,path);
    else if(path==="/api/job-alerts"||path.startsWith("/api/job-alerts/"))response=await alerts(request,env,path);
    else if(path==="/api/jobs"||path.startsWith("/api/jobs/"))response=await jobs(request,env,path);
    else if(path==="/api/applications"||path.startsWith("/api/applications/"))response=await applications(request,env,path);
    else if(path==="/api/interviews"||path.startsWith("/api/interviews/"))response=await interviews(request,env,path);
    else if(path==="/api/recruiter/jobs")response=await recruiterJobs(request,env);
    else if(path==="/api/admin/stats")response=await adminStats(request,env);
    else if(path==="/api/admin/questionnaire"||path.startsWith("/api/admin/questionnaire/"))response=await adminQuestionnaire(request,env,path);
    else response=await env.ASSETS.fetch(request);
    const headers=new Headers(response.headers);Object.entries(cors(request)).forEach(([key,value])=>headers.set(key,value));
    return new Response(response.body,{status:response.status,headers});
  }catch(error){if(error instanceof Response)return error;console.error(JSON.stringify({event:"api_error",path,error:String(error),stack:error?.stack}));return bad("Une erreur est survenue.",500)}
}};
