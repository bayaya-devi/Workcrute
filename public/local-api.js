(() => {
  const storageKey="workcrute.local.preview.v2",blank={accounts:[],session:null,documents:[],notifications:[],jobs:[],applications:[],applicationHistory:[],jobAlerts:[],savedJobs:[],interviews:[],settings:{}};
  const read=()=>{const stored=JSON.parse(localStorage.getItem(storageKey)||"{}");return {...structuredClone(blank),...stored}};
  const write=state=>localStorage.setItem(storageKey,JSON.stringify(state));
  const id=()=>crypto.randomUUID?.()||`${Date.now()}-${Math.random()}`;
  const fail=(message,status=400)=>{const error=new Error(message);error.status=status;throw error};
  const accountFor=state=>state.accounts.find(account=>account.id===state.session);
  const profileFor=account=>account.role==="candidate"?{first_name:account.firstName,last_name:account.lastName,phone:account.phone,city:"",region:"",country:"Maroc",preferred_language:"fr",professional_title:"",introduction:"",availability:"",availability_details:"",profile_visible:1,skills_json:"[]",preferences_json:"{}",experience_json:"[]",education_json:"[]",languages_json:"[]",questionnaire_answers:"{}",...(account.profile||{})}:{first_name:account.firstName,last_name:account.lastName,phone:account.phone,company_name:account.companyName||"",job_title:account.jobTitle||"",company_sector:account.companySector||"",company_size:account.companySize||"",city:account.city||"",website:account.website||""};
  const currentUser=state=>{const account=accountFor(state);if(!account)fail("Authentification requise.",401);return account};
  async function request(input,options={}){
    const url=new URL(input,location.origin),path=url.pathname,method=options.method||"GET",body=typeof options.body==="string"?JSON.parse(options.body||"{}"):options.body||{},state=read();
    if(path==="/api/public/stats")return {stats:{candidates:state.accounts.filter(x=>x.role==="candidate").length,companies:0,jobs:state.jobs.filter(x=>x.status==="published").length,applications:state.applications.length}};
    if(path==="/api/auth/register"&&method==="POST"){if(!["candidate","recruiter"].includes(body.role))fail("Type de compte invalide.");if(state.accounts.some(x=>x.email===String(body.email||"").toLowerCase()))fail("Un compte existe déjà avec cette adresse e-mail.",409);const account={id:id(),email:String(body.email||"").toLowerCase(),password:body.password,role:body.role,firstName:body.firstName,lastName:body.lastName,phone:body.phone,companyName:body.companyName||"",jobTitle:body.jobTitle||"",profile:{preferred_language:body.language||"fr"}};state.accounts.push(account);state.session=account.id;state.notifications.unshift({id:id(),user_id:account.id,type:"account",title:"Bienvenue sur Workcrute",body:"Complétez votre profil pour améliorer vos opportunités.",created_at:new Date().toISOString(),read_at:null});write(state);return {user:{id:account.id,email:account.email,role:account.role}}}
    if(path==="/api/auth/login"&&method==="POST"){const account=state.accounts.find(x=>x.email===String(body.email||"").toLowerCase()&&x.password===body.password);if(!account)fail("Identifiants invalides.",401);state.session=account.id;write(state);return {user:{id:account.id,email:account.email,role:account.role}}}
    if(path==="/api/auth/logout"&&method==="POST"){state.session=null;write(state);return {ok:true}}
    if(path==="/api/auth/forgot-password"&&method==="POST")return {ok:true};
    const account=currentUser(state),profile=profileFor(account);
    if(path==="/api/auth/me")return {user:{id:account.id,email:account.email,role:account.role,emailVerified:false},profile};
    if(path==="/api/profile"&&method==="PATCH"){if(account.role==="candidate"){account.firstName=body.firstName||account.firstName;account.lastName=body.lastName||account.lastName;account.phone=body.phone||account.phone;account.profile={...account.profile,city:body.city||"",region:body.region||"",country:body.country||"",preferred_language:body.language||"fr",professional_title:body.professionalTitle||"",introduction:body.introduction||"",availability:body.availability||"",availability_details:body.availabilityDetails||"",skills_json:JSON.stringify(body.skills||[]),preferences_json:JSON.stringify(body.preferences||{}),experience_json:JSON.stringify(body.experience||[]),education_json:JSON.stringify(body.education||[]),languages_json:JSON.stringify(body.languages||[])}}write(state);return {profile:profileFor(account)}}
    if(path==="/api/candidate/stats"){const apps=state.applications.filter(x=>x.candidate_user_id===account.id);return {applicationsSent:apps.length,applicationsActive:apps.filter(x=>["reviewing","shortlisted","interview"].includes(x.status)).length,interviews:state.interviews.filter(x=>x.candidate_user_id===account.id&&new Date(x.starts_at)>new Date()).length,recruiterViews:0}}
    if(path==="/api/candidate/overview"){const apps=state.applications.filter(x=>x.candidate_user_id===account.id),stats={applicationsSent:apps.length,applicationsActive:apps.filter(x=>["reviewing","shortlisted","interview"].includes(x.status)).length,interviews:state.interviews.filter(x=>x.candidate_user_id===account.id&&new Date(x.starts_at)>new Date()).length,recruiterViews:0};return {profile,stats,notifications:state.notifications.filter(x=>x.user_id===account.id).slice(0,5),applications:apps.slice(0,5),interviews:state.interviews.filter(x=>x.candidate_user_id===account.id&&new Date(x.starts_at)>new Date()).slice(0,3),recommendedJobs:state.jobs.filter(x=>x.status==="published").slice(0,4),matchingScore:null}}
    if(path==="/api/jobs"&&method==="GET"){const q=(url.searchParams.get("q")||"").toLowerCase(),city=(url.searchParams.get("city")||"").toLowerCase(),domain=(url.searchParams.get("domain")||"").toLowerCase(),contract=url.searchParams.get("contract")||"";return {items:state.jobs.filter(x=>x.status==="published"&&(!q||`${x.title} ${x.description} ${x.domain}`.toLowerCase().includes(q))&&(!city||String(x.city).toLowerCase().includes(city))&&(!domain||String(x.domain).toLowerCase().includes(domain))&&(!contract||x.contract_type===contract)).map(x=>({...x,is_saved:state.savedJobs.some(s=>s.user_id===account.id&&s.job_offer_id===x.id)}))}}
    if(path==="/api/jobs"&&method==="POST"){if(account.role!=="recruiter")fail("Accès recruteur requis.",403);const job={id:id(),recruiter_user_id:account.id,title:body.title,domain:body.domain,description:body.description,missions:body.missions||"",required_skills:JSON.stringify(body.skills||[]),contract_type:body.contractType,city:body.city,work_mode:body.workMode,company_name:body.companyName||account.companyName||"Entreprise",status:body.status==="published"?"published":"draft",published_at:new Date().toISOString()};state.jobs.unshift(job);write(state);return {job:{id:job.id,status:job.status}}}
    if(/^\/api\/jobs\/[^/]+$/.test(path)&&method==="GET"){const job=state.jobs.find(x=>x.id===path.split("/").pop()&&x.status==="published");if(!job)fail("Offre introuvable.",404);return {job:{...job,is_saved:state.savedJobs.some(s=>s.user_id===account.id&&s.job_offer_id===job.id)},matchingScore:null}}
    if(path==="/api/saved-jobs"&&method==="GET")return {items:state.savedJobs.filter(x=>x.user_id===account.id).map(saved=>state.jobs.find(job=>job.id===saved.job_offer_id)).filter(Boolean).map(job=>({...job,is_saved:1}))};
    if(path.startsWith("/api/saved-jobs/")&&method==="POST"){const jobId=path.split("/").pop();if(!state.savedJobs.some(x=>x.user_id===account.id&&x.job_offer_id===jobId))state.savedJobs.push({user_id:account.id,job_offer_id:jobId,created_at:new Date().toISOString()});write(state);return {saved:true}}
    if(path.startsWith("/api/saved-jobs/")&&method==="DELETE"){const jobId=path.split("/").pop();state.savedJobs=state.savedJobs.filter(x=>!(x.user_id===account.id&&x.job_offer_id===jobId));write(state);return null}
    if(path==="/api/job-alerts"&&method==="GET")return {items:state.jobAlerts.filter(x=>x.user_id===account.id)};
    if(path==="/api/job-alerts"&&method==="POST"){if(!body.keywords&&!body.domain&&!body.city&&!body.contractType&&!(body.skills||[]).length)fail("Choisissez au moins un critère.");const alert={id:id(),user_id:account.id,name:body.name||"",keywords:body.keywords||"",domain:body.domain||"",city:body.city||"",contract_type:body.contractType||"",skills_json:JSON.stringify(body.skills||[]),frequency:body.frequency||"daily",is_active:1,created_at:new Date().toISOString()};state.jobAlerts.unshift(alert);write(state);return {alert}}
    if(path.startsWith("/api/job-alerts/")&&method==="PATCH"){const alert=state.jobAlerts.find(x=>x.id===path.split("/").pop()&&x.user_id===account.id);if(alert&&typeof body.active==="boolean")alert.is_active=body.active?1:0;write(state);return {ok:true}}
    if(path.startsWith("/api/job-alerts/")&&method==="DELETE"){state.jobAlerts=state.jobAlerts.filter(x=>!(x.id===path.split("/").pop()&&x.user_id===account.id));write(state);return null}
    if(path==="/api/applications"&&method==="GET")return {items:state.applications.filter(x=>x.candidate_user_id===account.id)};
    if(path==="/api/applications"&&method==="POST"){if(state.applications.some(x=>x.candidate_user_id===account.id&&x.job_offer_id===body.jobId))fail("Vous avez déjà postulé à cette offre.",409);const job=state.jobs.find(x=>x.id===body.jobId&&x.status==="published");if(!job)fail("Offre introuvable.",404);const application={id:id(),job_offer_id:job.id,job_id:job.id,candidate_user_id:account.id,status:"submitted",title:job.title,description:job.description,company_name:job.company_name,city:job.city,contract_type:job.contract_type,work_mode:job.work_mode,cover_letter:body.coverLetter||"",created_at:new Date().toISOString(),updated_at:new Date().toISOString()};state.applications.unshift(application);state.applicationHistory.push({application_id:application.id,status:"submitted",created_at:application.created_at});write(state);return {application}}
    if(/^\/api\/applications\/[^/]+$/.test(path)){const application=state.applications.find(x=>x.id===path.split("/").pop()&&x.candidate_user_id===account.id);if(!application)fail("Candidature introuvable.",404);if(method==="GET")return {application,timeline:state.applicationHistory.filter(x=>x.application_id===application.id)};if(method==="PATCH"){application.status="withdrawn";application.updated_at=new Date().toISOString();state.applicationHistory.push({application_id:application.id,status:"withdrawn",created_at:application.updated_at});write(state);return {ok:true}}}
    if(path==="/api/documents"&&method==="GET")return {items:state.documents.filter(x=>x.user_id===account.id)};
    if(path.startsWith("/api/documents/")&&path.endsWith("/default")&&method==="PATCH"){const documentId=path.split("/").at(-2);state.documents.filter(x=>x.user_id===account.id&&x.kind==="cv").forEach(x=>x.is_default=x.id===documentId?1:0);write(state);return {ok:true}}
    if(path.startsWith("/api/documents/")&&method==="DELETE"){state.documents=state.documents.filter(x=>!(x.id===path.split("/").pop()&&x.user_id===account.id));write(state);return null}
    if(path==="/api/interviews"&&method==="GET")return {items:state.interviews.filter(x=>x.candidate_user_id===account.id)};
    if(path.startsWith("/api/interviews/")&&method==="PATCH"){const interview=state.interviews.find(x=>x.id===path.split("/").pop()&&x.candidate_user_id===account.id);if(interview)interview.status=body.status;write(state);return {ok:true}}
    if(path==="/api/notifications"&&method==="GET")return {items:state.notifications.filter(x=>x.user_id===account.id)};
    if(path==="/api/notifications"&&method==="POST"){state.notifications.filter(x=>x.user_id===account.id).forEach(x=>x.read_at=new Date().toISOString());write(state);return null}
    if(path.startsWith("/api/notifications/")&&method==="PATCH"){const note=state.notifications.find(x=>x.id===path.split("/").pop()&&x.user_id===account.id);if(note)note.read_at=body.read?new Date().toISOString():null;write(state);return {ok:true}}
    if(path.startsWith("/api/notifications/")&&method==="DELETE"){state.notifications=state.notifications.filter(x=>!(x.id===path.split("/").pop()&&x.user_id===account.id));write(state);return null}
    if(path==="/api/candidate/settings"&&method==="GET")return {settings:{inAppEnabled:true,emailEnabled:true,jobAlertsEnabled:true,profileViewEnabled:false,profileVisible:Boolean(profile.profile_visible),language:profile.preferred_language,...(state.settings[account.id]||{})}};
    if(path==="/api/candidate/settings"&&method==="PATCH"){state.settings[account.id]=body;account.profile={...(account.profile||{}),profile_visible:body.profileVisible?1:0,preferred_language:body.language||"fr"};write(state);return {ok:true}}
    if(path==="/api/auth/change-password"&&method==="POST"){if(account.password!==body.currentPassword)fail("Le mot de passe actuel est incorrect.",401);if(body.newPassword!==body.confirmPassword)fail("Les mots de passe ne correspondent pas.");account.password=body.newPassword;write(state);return {ok:true}}
    if(path==="/api/recruiter/jobs"&&method==="GET")return {items:state.jobs.filter(x=>x.recruiter_user_id===account.id)};
    fail("Cette action n’est pas disponible.",405);
  }
  async function uploadDocument(form){const state=read(),account=currentUser(state),file=form.get("file"),kind=String(form.get("kind")||"cv");if(!file?.size)fail("Document vide.");if(file.size>8*1024*1024||!/\.(pdf|docx?)$/i.test(file.name))fail("Document invalide.");const firstCv=kind==="cv"&&!state.documents.some(x=>x.user_id===account.id&&x.kind==="cv"),buffer=await file.arrayBuffer(),bytes=new Uint8Array(buffer);let binary="";for(let i=0;i<bytes.length;i+=0x8000)binary+=String.fromCharCode(...bytes.subarray(i,i+0x8000));state.documents.unshift({id:id(),user_id:account.id,kind,original_name:file.name,content_type:file.type,size_bytes:file.size,is_default:firstCv?1:0,created_at:new Date().toISOString(),download_url:`data:${file.type};base64,${btoa(binary)}`});write(state);return {ok:true}}
  window.workcruteLocalApi={request,uploadDocument};
})();
